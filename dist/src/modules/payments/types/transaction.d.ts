export declare class Transaction {
    id: string;
    reference: string;
    status_transaction: string;
    quantity: number;
    constructor(id: string, reference: string, status_transaction: string, quantity: number);
}
